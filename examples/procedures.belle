declare	bProc test();
declare	bProc test2(t bInteger);

implement{ 
	start;
		bPrint("hallo");
		bPrint(", World\n");
	end;
for test();

implement{
	start;
		bPrint(t);
	end;
for test2(t bInteger);

call test();
call test2(1);
