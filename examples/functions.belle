declare bFunc test() : bInteger;
declare	bFunc test2(t bInteger) : bInteger;
declare	bVar name : bAny;

implement{
	start;
		bPrint("value: ");
		bScan(name);
		bReturn(name);
	end;
for test() : bInteger;

impment{
	start;
		bPrint("value: ");
		bReturn(t);
	end;
for test2(t bInteger) : bInteger;

call test();
call test2(2);
